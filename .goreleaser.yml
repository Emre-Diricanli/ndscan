# .goreleaser.yml
# https://goreleaser.com/customization/
version: 2

project_name: ndscan

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/ndscan
    binary: ndscan
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - darwin
      - linux
      # - windows   # uncomment when you want Windows builds
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    # Only the binary goes in the archive (the default); the installer
    # extracts it from the tarball root.

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request

# Release configuration
release:
  github:
    owner: Emre-Diricanli
    name: ndscan
  draft: false
  replace_existing_draft: true
  # Overwrite assets instead of colliding with them. A re-run — or a retag
  # after a history rewrite — otherwise fails with 422 already_exists on every
  # artifact, even though the release itself is fine.
  replace_existing_artifacts: true
  mode: replace
  header: |
    ## ndscan {{ .Tag }}

    ### Installation

    ```bash
    curl -fsSL https://raw.githubusercontent.com/Emre-Diricanli/ndscan/main/install.sh | bash
    ```

# Optional: you can also produce Homebrew formula later
# brews:
#   - name: ndscan
#     homepage: "https://github.com/Emre-Diricanli/ndscan"
#     description: "Fast CLI + TUI for network discovery (nmap wrapper)"
#     license: "MIT"
#     tap:
#       owner: Emre-Diricanli
#       name: homebrew-tap
#     url_template: "https://github.com/Emre-Diricanli/ndscan/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
#     install: |
#       bin.install "ndscan"
#     test: |
#       system "#{bin}/ndscan", "--version"
