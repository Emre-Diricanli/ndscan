# build output
/ndscan
/dist/

# goreleaser
/goreleaser

# common editor / os files
.DS_Store
*.swp
.idea/
.vscode/

# local design specs — never publish these
# Working notes from brainstorming sessions. They are drafts, not documentation,
# and every push to main auto-publishes a release. Tracked here rather than in
# .git/info/exclude so the rule survives a re-clone.
/docs/superpowers/
